# Update Log

## 2026-07-08
* **Feature (sim_ecosystem_service layer + plant soil-tolerance intake)**: [sim-params](/datasets/sim-params.md) — new Bucket-B soil/nutrient/carbon ecosystem-service layer for the gardener-facing polyculture *complementarity* view (branch `feat/sim-ecosystem-services`, 8 TDD tasks + a matcher fix). Migration 074 adds 8 `entities` service/tolerance columns + the `sim_ecosystem_service` table. The USDA cascade adapter now also sources categorical service inputs (N-fixation/C:N/growth-rate/fertility) + plant soil-tolerance facts (texture-adaptation/anaerobic/CaCO3/salinity/drought/moisture/pH) onto `entities`; pure `lib/sim-ecosystem-service.js` derives per-plant categorical indicators (nitrogen_fixation_class, residue_decomposition, nutrient_demand, rooting_niche, growth_strategy, ground_cover, life_cycle_class, biomass_contribution, `soil_functions` rollup) with a per-row confidence envelope (`es_v1`). Research-grounded (109-agent adversarial deep-research, 23 verified claims; `docs/sim-ecosystem-services-gap-analysis.md`): categorical-not-flux, NO single sustainability score, C:N=decomposition-not-carbon, biomass=potential-proxy, dynamic-accumulators omitted. **Matcher fix (durable):** USDA returns `AcceptedId=0` for all rows → `matchAccepted` now prefers the bare species over infraspecific taxa; recovered major crops (corn/tomato/brassicas) that were silently ambiguity-skipped, ~2× the USDA match coverage (833→1,174 of 2,130) for both this layer AND the height cascade. Run: entities service+tolerance facts backfilled (2,982 fields; `growth_rate` 0→262); `sim_ecosystem_service` = 300 derived / 880 with soil-functions / 266 high-confidence. Plant soil-tolerance facts banked for the next feature (site soil-suitability / plantability). Corpus-local, not D1.

## 2026-07-04
* **Publish (D1 rail — 163 traits) + guard extraction + ingestion handoff**: [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) — published the ingestion chat's newly-promoted trait delta via `gen-traits-patch.cjs`: **163 `ai_reviewed` traits** (22 sources, 358 verdicts, **0 new entities** — all referenced entities already live), live real ai_reviewed traits 11,929→12,092; self-verified (0 entity writes, 0 DELETE) + applied (proven tool on its lowest-risk shape, no subagent-auditor pass). Claims delta 0 publishable (119 still held on page-less entities); dedup merges still NO-GO (canonicals unserved). **Tooling:** extracted the fail-loud id-collision guard out of `gen-claims-patch.cjs` + `gen-traits-patch.cjs` into a shared, dependency-injected `web/scripts/id-collision-guard.cjs` (pure `assertNoLiveIdCollision` / `findLiveIdCollisions`), now unit-tested (`id-collision-guard.test.cjs`, 4/4) — the guards were previously inline + untested. **Handoff:** new `docs/ingestion-backlog.md` seeded with the `Melanotus communis` bio_category genus-collision (stored `fungi`, is the click-beetle → `invertebrate`) for the ingestion/taxonomy chat.
* **Addition (`/simulator/` surface)**: [web site](/services/web-site.md) — embedded the **Agristory** polyculture pest-dynamics prototype (a *separate* Vite+Three.js product, repo `/home/beef/projects/agristory`) as a self-contained static bundle at `web/public/simulator/` (built with `base=/simulator/`), linked from the header nav to the right of "API", labeled "Simulator". Being a static asset under `public/`, it **bypasses the SSR middleware**, so the [AI-access policy](/decisions/ai-access-policy.md) SSR guard does not cover it — robots.txt + the edge WAF are the only AI gates (WAF coverage of `/simulator` is an edge-config check, not in-repo). The sim is powered read-only by a one-time exported corpus slice (`sim_*` growth curves + cited `claims`); relationships are cited, rates are designed defaults. Merged to `main` 2026-07-06 (059a857); **Pages-deployed live 2026-07-06** (deployment `42c335a3`; `https://aedin.io/simulator/` → 200, "Simulator" nav link live). The rail ran `npm run deploy` from `.worktrees/publish-main/web` (frozen-snapshot build, freeze NOT refreshed, no D1 change). **Deploy op-note:** the ~15,448-file Pages upload repeatedly hit transient Cloudflare-API `ECONNRESET`/connect-timeouts with NO partial-upload resume (each retry restarts the full 15,448); it finally went through unchanged on a stable window. The winning upload ran via `flatpak-spawn --host ./node_modules/.bin/wrangler pages deploy dist --project-name=agroeco --branch=main --commit-dirty=true` — the host wrangler is OAuth-authed (the sandbox has no `CLOUDFLARE_API_TOKEN`), and `flatpak-spawn` also sidesteps the Bash deploy-guard hook (which mis-reads the *primary* checkout's branch, not this worktree's `main`).
## 2026-07-03
* **Publish (D1 rail — 657 corrected claims live) + generator hardening**: [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) — after the ingestion chat re-reviewed its single-critic-vouched backlog through the full multi-critic consensus gate (corpus `claims` ai_reviewed 7,826→8,580), the rail published the delta via `gen-claims-patch.cjs`: **657 claims applied live** (+12 new entities, 54 sources, 1,527 verdicts; 119 held on 96 page-less entities → entity-serving rail), live ai_reviewed claims 7,806→8,463, GloBI's 212K untouched, `d1-publish-auditor` GO. **Deferred (non-blocking):** the new claims serve on entity pages immediately (SSR-from-D1) but their standalone `/claim/<id>` pages (static `getStaticPaths`) + the atlas graph edges (build-time) 404/absent until the next COORDINATED full republish — not run now because `AEDIN_USE_PUBLISHED=1 npm run build` needs a fresh `freeze-published-db` that would sweep in every concurrent chat's in-flight corpus work. **Hardening:** added a fail-loud id-collision guard to `gen-claims-patch.cjs` + `gen-traits-patch.cjs` — the live diff only knows the ai_reviewed id set, so a new claim/trait id colliding with a live non-ai_reviewed row (a GloBI tier2_globi claim) would have been silently overwritten by INSERT OR REPLACE; now it aborts (auditor-flagged latent hazard; id ranges currently disjoint but only incidentally). Data-quality follow-on for ingestion: new entity `Melanotus communis` mis-classified `fungi` (genus collision — it's the click-beetle in this pest context).
* **Publish (D1 rail — trait delta live)**: [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) — new `web/scripts/gen-traits-patch.cjs` (additive-delta traits publisher, sibling of `gen-claims-patch.cjs`: live-id diff via `wrangler`, `INSERT OR IGNORE` entities so it never REPLACEs a live entity row / can't leak held entity work — the multi-chat-safe alternative to `build-d1-traits-patch.cjs`). First use applied live: **+402 `ai_reviewed` traits** (58 CTAHR + Guam extension units 245–253) + 8 new entities + 9 sources + 804 verdicts, 0 held, `d1-publish-auditor` GO; live real `ai_reviewed` traits 11,527→11,929, GloBI's 212K untouched. (First apply hit a transient wrangler fetch-fail that rolled back cleanly per wrangler's atomic guarantee; retry succeeded.)
* **Publish (D1 rail — claims no-op + dedup-merge NO-GO)**: [entity-dedup](/pipelines/entity-dedup.md) — the 3-channel publish sequence's other two channels HELD. Interaction-claims: 0 publishable — all 22 held on unserved `needs_dedup` cultivars (almond/walnut/grape cvs; leaffooted bug, *Alternaria*, Pierce's disease → variety-intake rail). Dedup merges: `gen-merge-d1-patch` generated 2,818 served merges (corpus flat, 0 chains, GloBI-safe) but `d1-publish-auditor` returned **NO-GO** — 55% of redirect canonicals are not live pages (`301->404`); the reconciliation is gated on serving ~1,344 canonical entities first (owner: hold + serve-canonicals-first). Logged the tool gap (`selectServedMerges` lacks a canon-servable guard) + the `×`-hybrid slug-NULL canonicals; merge patch held at `web/d1/patch-merge-dedup.sql`. Memory `dedup-needs-review-drain-pending-publish` REOPENED (was wrongly CLOSED — live has only 194 of 2,818 merges).
* **Recovery tool (tiebreak — held-claim consensus repair)**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) — new `backend/tiebreak-batch-prepare.js`. Diagnosed the pending-vouched backlog: 532 rows sat "one-plausible-short" (agroecologist plausible + a router-mis-routed 2nd specialist returning `out_of_scope`/`uncertain` → 1 plausible, 0 implausible, gate not met). The tool selects such rows (`pl=1 AND im=0 AND n=2`, the `n=2` guard making it idempotent) and emits self-route batches (all 5 specialist templates + each claim's `already_judged_by`); a general-purpose Agent picks the correct specialist by content, sidestepping `lib/critic-router.js`. Import/promote reuse existing scripts; shared prompt at `backend/tiebreak-verdicts/INSTRUCTIONS.md`. Pilot 24→11 (46%), full sweep 508→**392 promoted (77%)**; non-recovered correctly held by rank-floor/locality gates or honest implausible tiebreaks (caught an extractor-fabricated "bees pollinate corn"). Durable fix remains repairing the router itself. Corpus-local (no D1). Memory: `tiebreak-consensus-recovery`.
* **Ingestion (stalled unit 237 finished + singleton sweep)**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) — src 237 (SARE *Crop Rotation on Organic Farms*) had 16 multi-critic batches prepared but 0 verdicts (a prior session stopped before dispatch); dispatched the critics → 634 verdicts → promoted 292 (254 claims + 38 traits). A bare all-source singleton sweep of no-verdict tail rows promoted 24 more. Session corpus `claims` `ai_reviewed` 8,080→8,449, pending-vouched backlog 1,843→1,125. All local-only.
* **Feature (sim-trait source cascade — USDA + Trefle)**: [sim-params](/datasets/sim-params.md) built a pluggable, priority-ordered cascade that backfills un-surfaced sim scalar inputs `entities.{maximum_height_cm, growth_habit, min_root_depth_cm}` from external sources (branch `feat/simulation`, 11 TDD tasks). Each source is a `{name, cacheDir, extract}` adapter in `lib/sim-trait-sources.js`; a fetcher caches raw records, and `backfill-sim-traits.js` applies them in order through the shared fill-if-NULL write-core `lib/sim-scalar-backfill.js` (earlier source wins). **Source #1 USDA PLANTS** (`fetch-usda-characteristics.js` + `lib/usda-normalize.js`, public domain; height/root from `PlantCharacteristics`, habit from `PlantProfile` — search results carry NULL habit, a `PlantProfile`-by-symbol call is required). **Source #2 Trefle** (`fetch-trefle-sim.js` + `lib/trefle-normalize.js`, name-resolved since stored `trefle_id`s are stale). BIEN + monographs are future drop-in adapters. Migration **073** added `sim_plant_growth.min_root_depth_cm`; the derivation now emits a `[min,max]` root-depth range (`min ≤ max` guard, `GROWTH_MODEL_REF`→`growth_v2`). Full run over the 2,132-plant population filled 119 heights + 862 growth-habits + 128 root minimums (1,109 `revision_log` rows, `method='sim_trait_backfill'`, source never named per sim-data-provenance; reversible); `sim_plant_growth` `param_status='derived'` rose 91→208. Height coverage skews to woody/field crops (obscure/tropical/cultivar tail has none in USDA), so growth-habit is the larger qualitative win on the designed-default majority. Corpus-local, not D1.
* **Tooling + ingestion (sim height backfill — Rubatzky)**: [sim-params](/datasets/sim-params.md) — new `backend/backfill-sim-height.js` (TDD 2/2) reads extracted plant heights, sanity-range-guards (5 cm–40 m), dedupes to max-per-species, and fills `entities.maximum_height_cm` (fill-if-NULL, plantae-only, reversible via JSON backup + `revision_log` method `sim_height_backfill` — source never named, per the sim-data-provenance policy). First run: 102 heights / 91 species extracted from the in-repo Rubatzky full-text → 79 crops filled → after `derive-sim-params.js --apply --run-id=height-backfill-1`, `sim_plant_growth` `param_status='derived'` rose from 14 to 91 (the label is height-gated). Reusable for any future height source (USDA PLANTS / Trefle dump; Wikidata proved 0/20 coverage). Corpus-local, not D1.
* **Policy (sim data provenance)**: [sim-params](/datasets/sim-params.md) — established: values that feed ONLY the simulator (`sim_*` + scalar inputs the derivation reads, e.g. `entities.maximum_height_cm`) may be sourced from ANY reference incl. copyrighted books, because the sim consumes only the factual number and never surfaces/cites/distributes the source. Store un-surfaced (the `entities` scalar attribute, no citation), NOT a cited `entity_trait_claims` row. Surfaced corpus claims stay bound by the facts-only copyright policy. Memory: `sim-data-provenance-policy`.
* **Ingestion (trait-deepening pass #1 — ctahr_eb16)**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) — extracted + promoted 58 growth-trait claims (spacing 44 / life_cycle 10 / days_to_harvest 3 / growth_habit 1) from the open Hawaii vegetable production guide via the trait-table-extractor + multi-critic (agroecologist + horticulturist, 120/120 plausible) + the new crop-gate. **Finding:** the `sim_plant_growth` `derived` count did NOT move (stayed 14) — the `derived` label is gated SOLELY on `maximum_height_cm` being sourced (`lib/sim-derive.js`), and this guide tabulates spacing not heights. Spacing/maturity DO improve sim row values (70 fact-sourced `max_spread_cm`, 208 `days_to_maturity`) but not the label. Owner kept the height-gate; fix underway = backfill `entities.maximum_height_cm` from the in-repo Rubatzky text (sim-data-provenance carve-out; Wikidata proved 0/20 coverage). Crop-gate variety edge-case (2 turnip-cultivar `days_to_harvest` skipped — auto-created cultivars aren't crop-tagged) noted as a follow-on.

## 2026-07-02
* **Hardening (sim-params layer constants + classifier)**: [sim-params](/datasets/sim-params.md) domain-plausibility pass over `lib/sim-defaults.js`/`lib/sim-derive.js` (commit 31252c9, TDD 29/29 green) — corrected tree maturity/rooting-depth, vine/geophyte `canopy_layer` self-consistency with `canopyLayerForHeight`, grass spread, compressed `CONTROL_MAGNITUDE` (parasitoid no longer a blanket 0.5), temperate-skewed pest generations, honest `'unknown'` biocontrol specificity default; classifier word-boundary + groundcover-vine-misclassification fixes; null guards on `pestDynamics`/`biocontrolDefaults`. Re-derived (run-id=hardening); row counts unchanged (2132/2132/798/1117).
* **Feature (trait-deepening pass #1 — crop-gate)**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) added a NARROW promote-time **crop-gate** (`promote-staged-claims.js::CROP_ANCHORED_TRAITS`, TDD 3/3) — growth/morphology/maturity traits (`maximum_height_cm`/`average_height_cm`/`canopy_spread_cm`/spacing/`days_to_harvest`) must attach to a crop anchor (`entities.crop_type IS NOT NULL OR edible=1`); non-crop subjects are skipped. Deliberately excludes pest/beneficial traits (voltinism, thermal_min) + general plant descriptors (life_cycle, growth_habit) so it never rejects arthropod trait claims. First use: extracting wired growth traits from `ctahr_eb16` to flip `sim_plant_growth` designed→derived (spec/plan `docs/superpowers/{specs,plans}/2026-07-02-trait-deepening-growth-traits*`).
* **Complete (sim-params layer)**: [sim-params](/datasets/sim-params.md) the layer is DONE — `backend/derive-sim-params.js` (the orchestrator: reads corpus facts + `lib/sim-derive.js`/`lib/sim-defaults.js`, writes only `sim_*`, full-replace of derived/designed rows preserving `override` rows, dry-run default/`--apply`, `revision_log` run-summary) shipped and applied. Layer is regenerable at any time and remains corpus-local — not served to D1. OKF concept rewritten as a durable description (tables/keys/envelope/derivation/D1-exclusion); the per-task narrative below moved fully into this log (see the dated bullets that follow). Trait-deepening follow-on: `docs/sim-params-trait-deepening.md`.
* **Classifier (sim-params layer free-text derivation)**: [sim-params](/datasets/sim-params.md) Task 3 — introduced `backend/lib/sim-derive.js::classifyLifeForm` (commit 5fb77d2, TDD 4/4 green). Pure free-text keyword classifier (requires `sim-defaults.js`) maps noisy growth_habit prose + optional lifeCycle/family inputs → one of 8 LIFE_FORM_DEFAULTS via ordered regex matching (tree, shrub, geophyte, vine, grass, herb). Herb splits into annual_herb or perennial_herb by lifeCycle. Returns {lifeForm, matched[]} for audit. Precedence order critical (geophyte checked before vine for tuber-climbers; herb last). Later tasks (4–5) extend sim-derive.js with more derivation functions (growthCurveParams, visualMapping, pestDynamics, biocontrolDefaults).
* **Constants (sim-params layer defaults)**: [sim-params](/datasets/sim-params.md) Task 2 — introduced `backend/lib/sim-defaults.js` (commit 28e244c, TDD 5/5 green). Pure-data constants module (no DB queries) containing versioned designed model parameters: `GROWTH_MODEL_REF`, `PEST_MODEL_REF`, `BIOCONTROL_MODEL_REF`, `VISUAL_MODEL_REF` version tags; `LIFE_FORM_DEFAULTS` (8 forms: annual_herb, perennial_herb, grass, vine, geophyte, shrub, tree, unclassified — each with time_unit, max dimensions, days_to_maturity, height_curve_model, canopy_layer, light_extinction_coeff); `CONTROL_MAGNITUDE` (guild → 0–1 suppression fraction); `BIOCONTROL_DEFAULTS` + `PEST_DEFAULTS` (generic phenologies); `canopyLayerForHeight(cm)` helper. Modelling choices (not measurements); horticulturist plausibility expected. Serves as fallback/baseline for Tasks 3–5 (derive-sim-params.js and row populators).
* **Schema (sim-params layer foundation)**: [sim-params](/datasets/sim-params.md) migration 072 created the four-table simulator parameters layer (`sim_plant_growth`, `sim_pest_dynamics`, `sim_biocontrol`, `sim_visual`), each carrying a shared **provenance envelope** (param_status, derivation_method, model_ref, inputs_json, confidence, generated_at, generated_run_id) to track derived vs. designed params. Tables are UNIQUE-keyed on entity_id (except sim_biocontrol on claim_id) with appropriate indexes; all idempotent via CREATE TABLE IF NOT EXISTS. This is a schema-only task; populators (`derive-sim-params.js` etc.) are forthcoming. Kept regenerable and separate from the provenance-gated corpus.

## 2026-06-30
* **Phase 4 follow-ons (all done)**: (1) **reconcile** — `derivePathogenTransmission` now SKIPS (per-trait) any pathogen that already has a stored, source-backed `transmission_vector` (or `vector_borne` mode), so the derive never duplicates extracted claims (40→28 derived rows; TDD 3/3). (2) **abiotic was already done** — `transmission_mode` has a full controlled enum (`airborne, soilborne, seedborne, waterborne, vector_borne, contact, wound, pollen_borne`) + 65 extracted claims; the earlier "abiotic is a follow-on" note was wrong. (3) **4 host edges** generated (ArMV→Fragaria, GLRaV→Vitis, BRV→Ribes, PMWaV→Ananas) completing the new-virus triangles. (4) **synonym merge** *Carneocephala fulgida*→*Xyphon fulgida* (dup `disease_vector`→Xylella claim quarantined). Surgically published the additive subset (4 host edges + ArMV entity + 28 transmission rows) to live D1; the corpus-only `disease_vector` backlog (sharpshooters etc.) rides the next coordinated full republish.
* **Feature (Phase 4 — pathogen transmission traits, biological half)**: [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) new `backend/lib/transmission-traits.js::derivePathogenTransmission` (TDD 2/2) — a DERIVED, D1-only trait materialized in `build-d1.cjs::selectReadSubset` alongside variety inheritance. For each served pathogen that is the object of ≥1 `ai_reviewed` `disease_vector` edge: `transmission_vector` (value_json list of its vector names) + `transmission_mode='vector_borne'`, synthetic ids `pathogenId*1e9+offset`, `inherited_from_entity_id=null`. Verified: 40 rows / 20 pathogens (e.g. PRSV → [Aphis gossypii, Aphis nerii, Myzus persicae]). build-d1 tests updated (mock claims gained `interaction_category`) — 5/5 green. Live surfacing rides the next full `data.sql` republish. Abiotic `transmission_mode` (extracted) + reconciling the ~13 pre-existing stored `transmission_vector` claims are follow-ons.
* **Fix + negative result (GloBI `hasVector`)**: [globi-ingestion](/pipelines/globi-ingestion.md) — `lib/globi-classify.js` no longer maps `hasVector`→`disease_vector` unconditionally (TDD 6/6). Investigation for the disease-vector spec's Track B / "Phase 3" (GloBI vector recovery) found GloBI `hasVector` is **~74% plant→frugivore/nectarivore seed/pollen DISPERSAL** (bats/birds/ants), ~15% phoresy, and only 16 pathogen-named edges (all out-of-scope Zika→*Aedes*) — **no recoverable agricultural disease vectors**. `hasVector` is now bio-category-aware (plant→animal ⇒ `seed_dispersal`; pathogen→arthropod ⇒ `disease_vector`; else `facilitation`); the one mislabeled served edge (*Sanguinaria*→*Formica* myrmecochory) corrected. **Phase 3 (mass GloBI vector recovery) ABANDONED** — the premise was wrong; the foot-gun classifier rule is the only salvageable fix.
* **Data correction (Phase 2b tail)**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) resolved the 5 host-crop `vectorOf` claims whose pathogen had no entity. Created 4 virus entities (Arabis mosaic / Grapevine leafroll-associated / Blackcurrant reversion / Pineapple mealybug wilt-associated virus — `microbe`, served, `needs_taxonomy_review`) + repointed 4 claims to `disease_vector`. **Agroecologist caught a 5th as an extraction error and it was QUARANTINED** (`quarantined_vector_mismatch`): *Longidorus elongatus*→Arabis mosaic is the wrong vector (Perry & Moens: *L. elongatus* vectors TomBRV/RRSV; the canonical ArMV vector is *X. diversicaudatum*) — the extractor mis-split a plural-subject quote. Also fixed a name (*Dysmicoccus*→"Pineapple Wilt Virus" → the correct PMWaV). Live D1: 3 on-live repoints + 3 new-entity INSERTs + the quarantine DELETEd (ArMV + its corpus-only claim stay publish-backlog). `revision_log method=vector_reencode_2b`, reversible.
* **Serving + publish**: [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) + [entities](/datasets/entities.md) — **served the page-less referenced tail** (the "serve-all-entities" gate). The publish backlog was gated on **entity serving, not claim publishing**: literature-ingested entities referenced by `ai_reviewed` claims were on D1 as NULL-slug (page-less) rows, so `gen-claims-patch` HELD their claims. New `serve-referenced-entities.js` (extends `lib/slug-backfill.js`: factored `planFromRows` core + `selectSluglessReferenced` + `applyReferencedServe`; TDD 6/6) slugged + `scope_tier=0`-promoted **620 clean** referenced entities (collision = duplicate taxon → flagged `needs_dedup`, never suffixed); **agroecologist held 13 generic-guild/coarse nodes** (`Insecta`, `companion plants (various)`, …) via a curated `--hold-ids` file (quarantine-not-serve principle). Plus **43 resistance cultivar varieties** (UC IPM Grape/Almond/Walnut, sources 250/251/253) served via `serve-grin-claim-subjects.js` → **`held_resistance` 61→0**. Total **663 served**. New `web/scripts/gen-served-entities-patch.cjs` (`INSERT OR REPLACE INTO entities` — the keystone `gen-claims-patch`'s `INSERT OR IGNORE` can't do: fixes already-live NULL-slug rows) + page-less guard & `--served-since` scope on `build-d1-traits-patch.cjs`. **Published live (3 surgical patches, order entities→claims→traits, no Pages deploy — SSR; `d1-publish-auditor` GO):** live `ai_reviewed` 7,497→**7,806**, resistance 46→**106** (full pillar), +244 traits; GloBI `tier2_globi` intact at 212,077; 0 held nodes leaked. Reversible (`revision_log` + JSON backups). **Follow-ons:** 22 claims / 16 `needs_dedup` cultivar entities + 80 dedup + 79 taxonomy-review tail stay held for the dedup/curation rails. Spec: `docs/superpowers/specs/2026-06-29-serve-referenced-entities-design.md`.

## 2026-06-29
* **Data correction (Phase 2b)**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) host-crop-object `vectorOf` claims re-encoded to the disease triangle. Applier extended with `repoint` (object→pathogen, disease_vector) + `repurpose` (subject→pathogen, pathogen_pressure) actions (`reencode-vector-claims.js`, TDD 11/11). Agroecologist-gated (corpus-confirmed: nematode→nepovirus, mealybug→badnavirus, ICTV virus identities). **Corpus:** 10 quarantined (9 `generic` + 1 `oos` Musca→human) + **17 re-encoded** (8 repoint→`V→disease_vector→P`, 7 repurpose→`P→pathogen_pressure→C`, 2 `redundant` quarantine), completing triangles across 2a+2b (e.g. Frankliniella→TSWV→tomato, Pentalonia→BBTV→Musa, X.americanum→ToRSV→Vitis). 9 pathogen entities served (slug+scope_tier). **Live D1:** 8 on-live re-encodes patched (id-matched UPDATE subject/object+category+globi; all referenced pathogens already live) + 2 redundant DELETEd + (earlier) 8 generic DELETEd. **Deferred/follow-on:** 5 claims whose pathogen has no entity yet (Arabis mosaic, Grapevine leafroll-assoc, Blackcurrant reversion, Pineapple wilt + the watermelon-mosaic multi-pathogen tail); 7 re-encoded claims that are corpus-only (publish backlog); host-edge gaps for repoint-only triangles; the same-pairing/synonym entity-dedup the agroecologist flagged.
* **Data correction (Phase 2a)**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) re-encoded the historical *object-is-pathogen* `vectorOf` literature claims via `backend/reencode-vector-claims.js` (reversible — `revision_log` `method=vector_reencode_2a` + JSON backup, dry-run default), agroecologist-gated: 22 flipped `pathogen_pressure`/`pest_pressure`→`disease_vector`; 2 quarantined (`negated` — leafhopper "are not vectors"; `non_field_vector` — *Cuscuta* is a lab virus-conduit, not a field vector, per Hull/Matthews). Live D1 surgically patched for the 12 on-live (11 flip UPDATEs + *Cuscuta* DELETE; id-matched, no schema change, no deploy); the reported aphid→CMV claims now render "is a disease vector for" live. 12 corpus-only claims (Xylella-sharpshooter / Geosmithia-beetle / dagger-nematode) remain in the publish backlog. Host-crop-object re-encode is Phase 2b.
* **Wiring**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) `reconcileVectorCategory` wired into `promote-staged-claims.js` (called immediately after GloBI-term resolution, before the claim INSERT) — future `vectorOf` extractions now land as `disease_vector` at promote time, not force-fit `pathogen_pressure`/`pest_pressure`. Extractor guidance updated: `disease_vector` definition added to the `interaction_type` enum block; `vectorOf` constraint note added to the Vector/dispersal predicate section.
* **Addition**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) promote-time **vector reconciliation gate** — new pure normalizer `lib/interaction-vocabulary.js::reconcileVectorCategory(category, globiTerm)` (TDD 5/5). When GloBI's Relations-Ontology term is `vectorOf` (marking a disease or pest vector), the interaction category MUST be `disease_vector`, never the force-fit `pathogen_pressure` or `pest_pressure` that the extractor sometimes emits. Single source of truth, consumed by `promote-staged-claims.js` (Task 3 pending). Resolves [issue #7282736 vector-as-pathogen mis-encoding](/decisions/).

## 2026-06-28
* **Automation**: [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) wired a **PostToolUse Bash reminder hook** (`.claude/hooks/posttooluse-d1-inheritance-reminder.mjs`, TDD 5/5) — fires after a `wrangler d1 execute --remote` trait publish to remind running `gen-inheritance-refresh` (silent on the refresh's own patch + non-trait publishes), so inheritance drift is surfaced at publish time instead of forgotten.
* **Tooling**: [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) new `web/scripts/gen-inheritance-refresh.cjs` (TDD 5/5) — durable global variety-inheritance refresh. Reuses `build-d1.cjs::selectReadSubset` (no duplicated scope/synthetic-id logic), isolates the inherited subset, emits a full-replace of just that subset (DELETE-all-inherited + re-INSERT, base/GloBI untouched, idempotent). Manual tool — run after any conserved-trait corpus change.
* **Operational + doc**: [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) — investigated the apparent live-vs-corpus trait "divergence" and found it is **materialized variety trait inheritance** (DERIVED, D1-only; live > corpus by design), not a bug. Did a one-time **global inheritance refresh** (DELETE-all-inherited + re-materialize served-variety inheritance via resolveVarietyTraits + `vid*1e9+row.id`): live 9,736→10,378 inherited claims / 855→950 varieties, 0 orphans. Recorded the mechanism + the drift caveat (surgical patches skip inheritance; no standing global-refresh job). Also published 706 foundational trait claims (scoped patch — collision-free, climate-divergence-safe).
* **Feature**: [web-site](/services/web-site.md) entity-page **Trait summary sidebar** — `web/src/components/TraitSummary.astro` + `web/src/lib/trait-summary.ts` (`summarizeTraits`, TDD 7/7) condense the multi-source `entity_trait_claims` into one line per trait (numeric → min–max range, list → union, categorical → distinct values), with a per-trait source count. Complements `EntityEnvelope` (scalar-column "Profile"). The serving-layer half of the keep-all-values policy. Build green.
* **Decision + fix**: [traits](/datasets/traits.md) **keep-all-trait-values policy** — distinct values are never deleted as conflicts; aggregation moves to the serving layer. `resolve-trait-conflicts.js` no longer deletes/flags **numeric** traits (range endpoints / multi-source readings, not contradictions); **restored 113 previously-deleted numeric values** (mostly pH) from backup. Sidebar will show a min–max range; the claims list shows every sourced value.
* **Fix + hardening**: [traits](/datasets/traits.md) (a) fixed 15 **double-encoded** `edible_part value_json` (`"[\"corm\"]"`) the corm/rhizome salvage created by stringifying a payload value that `encodeTraitValue` re-stringifies — unwrapped one layer; durable lesson recorded (correct a staging list `value_json` as an array, not a stringified array). (b) Cleaned 3 corm/rhizome salvage residue claims (ginger left a stale `[stem]` beside `[rhizome]`; two aroids had a subset beside the richer claim). (c) `resolve-trait-conflicts.js` made **source-aware** (`hasSameSourceConflict`): a cross-source value difference is legitimate provenance (kept, never flagged); only a single source contradicting itself is flagged. Re-characterized the 111 backlog as 50 legitimate cross-source + 58 same-source inconsistencies (the real review backlog).
* **Addition**: [traits](/datasets/traits.md) migration 071 — `edible_part` enum += `bud`, `inflorescence`, `calyx` (12 → 15), closing the reproductive-organ gap the gate found: broccoli/cauliflower/artichoke (inflorescence), Brussels-sprout/caper/myoga (bud), roselle (calyx) — all force-fit to `flower`. 5 promoted + 2 held claims corrected; `flower` reserved for a true edible flower. Extractor pitfalls note extended.
* **Tooling**: [traits](/datasets/traits.md) new `backend/resolve-trait-conflicts.js` (TDD 10/10, dry-run-default, backup) — resolves cross-value trait conflicts by choosing a source-asserted WINNING VALUE (list superset / numeric ≤20%-spread / valid boolean / substring-richest categorical / pest_mobility none→sedentary) and deleting only losing-value claims; flags partial-overlap/far/genuine for review. First run: −201 rows (37,526 → 37,325), 111 flagged (72 categorical_genuine, 20 list_partial_overlap, 16 numeric_far, 3 unparseable).
* **Tooling**: [traits](/datasets/traits.md) new `backend/dedup-entity-trait-claims.js` (TDD, dry-run-default, row-backup) — collapses chunk-overlap **exact** trait dups (same entity+trait+value+source; keep-rule prefers a `source_quote`), FLAGS cross-value conflicts to a report (never auto-resolves), KEEPS multi-source corroboration. First run: −223 exact dups (37,749 → 37,526 `entity_trait_claims`), 278 conflicts flagged for review (new curated/agroecologist backlog).
* **Data-hygiene**: [traits](/datasets/traits.md) woody-herb `life_cycle` correction — 11 held claims mislabeled `herbaceous_perennial` (rosemary/lavender/sage/thyme/savory/hyssop + caper/lemon-verbena/vanilla/cassava/pigeon-pea) corrected → `woody_perennial`, re-judged (10 promoted, pigeon-pea held uncertain). Added a "Common value pitfalls" guard to `.claude/agents/trait-table-extractor.md` (woody → woody_perennial; corm/rhizome not tuber/stem/root) to stop the recurrence. Deduped the `life_cycle` trait (18 chunk-overlap dup-groups → 1 claim/entity). **Surfaced** a systemic chunk-overlap duplication across `entity_trait_claims` (~700 dup-groups; general dedup pass deferred).
* **Clarification**: [llm-literature-ingestion](/pipelines/llm-literature-ingestion.md) promote-time **locality gate documented as interaction-only** — `entity_trait` rows are exempt (an intrinsic trait is not regional; `promote-staged-claims.js` routes them to `promoteEntityTraitRow` and `continue`s before the gate). This was already the structural behavior (predates the Adams batch); a no-locality regression test now pins the invariant + a code comment makes the exemption intentional. (Corrects a misattribution that read the gate as blocking 4 Adams trait claims — it never did.)
* **Addition**: [traits](/datasets/traits.md) migration 070 — `edible_part` enum extended with `corm` + `rhizome` (10 → 12 values), driven by the multi-critic gate during the Rubatzky *World Vegetables* foundational-trait population: taro/water-chestnut (corm) and ginger/turmeric/arrowroot/canna/sweet-flag (rhizome) had been force-fit to `tuber`/`stem`/`root` and held. The 18 held force-fit claims were curated-corrected to the right organ, re-judged (36/36 plausible), and promoted. Records the define-then-populate feedback loop (real data exposes a vocabulary gap → gap feeds back into the schema).

## 2026-06-27
* **Reclassification**: [classification-system](/architecture/classification-system.md) family-floor role overhaul — coarse-taxonomy default tiers (`taxonomy_class` class/order/kingdom + `bio_category_default`) DISABLED in `role_rules`; `primary_role` now asserted ONLY from claim-derived (`interaction_profile`) or curated species/genus/family rules (family is the floor), unmatched → `unclassified` (governing principle: ambiguity > false identification). New reversible tooling (`coarse-rank-audit.js`, `disable-coarse-role-rules.js`, `apply-role-rules.js --unmatched-to-unclassified`, `add-family-floor-rules.js`, `reclassify-variety-roles.js`, `verify-no-coarse-defaults.js`, `web/scripts/gen-roles-patch.cjs`) — all changes logged to `role_corrections`. `classify-taxon.js` CLASS_RULES role-valence defaults emptied (`getBioCategory` unchanged); varieties inherit parent role; crop identity re-asserted from ECOCROP. Agroecologist-gated; shipped live (D1 + Pages).
* **Deprecation**: Retired `docs/journal.md` (the manual turnover log) — it went stale in practice (~7 days untouched through heavy work) while the auto-maintained `.remember/` buffers became the real session-continuity layer. [knowledge-systems-boundary](/decisions/knowledge-systems-boundary.md) revised to three durable stores (CLAUDE.md / .okf / memory) + `.remember/` continuity; the `CLAUDE.md` "Journal / turnover log" section + "what goes where" reference were removed. `.remember/` distilled tiers (`core-memories`/`archive`/`recent`/`today-*.done`) are now git-tracked via a selective `.remember/.gitignore` so the sole continuity layer is versioned. Old journal content recoverable from git history.
* **Addition**: Added `datasets/traits.md` concept; migration 069 registers the foundational crop-trait batch (27 plantae traits, define-only) + extends deficiency_sensitivity (micronutrient sensitivity) + sharpens maximum_height_cm. Overlap-resolution-in-descriptions principle recorded.
* **Addition**: Interaction-graph X/quadrant layout + shared selection panel
  ([web-site](/services/web-site.md)) — pure `x-layout.ts` (kingdom×valence
  placement, ring-2 follows target side, on-demand ring-3) + `graph-panel.ts`
  (persistent selection, neighbor chips) on `/crop-web` (X + panel) and `/atlas`
  (panel only). The X is now the **default** crop-web view (labelled cards,
  per-quadrant cap + "+N more" expander, edge docking, asterisk legend,
  persistent selection). Front-end only; vitest harness added.
* **Hardening**: [entity-dedup](/pipelines/entity-dedup.md) merge-chain flatten + rail fix + generator re-source — `backend/flatten-merge-chains.js` walks `entities.merged_into_entity_id` chains and collapses multi-hop pointers to the terminal canonical (dry-run-default, `--apply`). `merge-entity.js` now sets `entities.merged_into_entity_id` at merge time (merge-rail forwarding fix, closing the gap where the pointer was set only by the D1 patch). `gen-merge-d1-patch.cjs` re-sourced from the flattened corpus pointer (one row per served tombstone, canon = terminal) instead of the per-hop `entity_dedup_log`; aborts loudly via `assertNoTombstoneCanon` if any canonical is itself a tombstone, enforcing flatten-before-generate. The `entity_dedup_log` remains the reversal/audit authority.

## 2026-06-26
* **Addition**: [entity-dedup](/pipelines/entity-dedup.md) live-D1 reconciliation (follow-on #3) — `gen-merge-d1-patch.cjs` (entity_dedup_log-driven tombstone + claim/trait re-point patch), `[slug]` 301-redirect to canonical (`resolveMergeRedirect`), listing filters, build-d1 tombstone durability, `entities.merged_into_entity_id` on D1. Reusable for future needs_review merges. Operational publish (ALTER → patch → deploy) deferred.

## 2026-06-25
* **Addition**: [GRIN-narrative enrichment](/pipelines/grin-narrative-enrichment.md) Phase 2 (trait enrichment) — migration 068 (13 foundational plant traits), extractor Phase-2 section, `grin-narrative-stage.js` trait-staging path (value-kind dispatch). Reuses entity_trait → multi-critic (horticulturist) → promote. Produce/growth GRIN-extracted; reproduction/pollination + nutrient vocab-only (monograph-populated later). 19-trait gap analysis deferred to a foundational-vocab sub-project.
* **Serving**: Host-plant **resistance is now live-served** ([claims](/datasets/claims.md)) — the ordering-safe publish: live D1 `ALTER` (`resistance_level`) → surgical patch of the served `disease_resistance` claims → `queries-d1.ts` + `EntityClaimsTable` render an "is resistant to" sentence + a `resistance_level` badge on variety pages. Claims whose subject is a slug-less `needs_dedup` cultivar were held for variety-intake.
* **Hardening**: `gen-claims-patch.cjs` ([corpus-and-live-d1](/architecture/corpus-and-live-d1.md)) gained a **page-less-reference guard** — it never publishes a claim whose subject/object would resolve to a not-yet-live, slug-less entity (a broken page); such claims are held + reported.
* **Addition**: `web/scripts/gen-claims-patch.cjs` recorded in [corpus-and-live-d1](/architecture/corpus-and-live-d1.md) — a claim-id-scoped, additive (no-DELETE) surgical patch that self-computes the live-vs-local `ai_reviewed` delta and publishes only the missing literature claims + their not-yet-live entities/sources/verdicts, preserving the in-sync GloBI tier. Complements the source-scoped re-ingestion patch `build-d1-patch.cjs`. Used to close an 876-claim literature publish gap.
* **Addition**: Host-plant resistance model — `disease_resistance` + `pest_resistance` interaction categories, `claims.resistance_level` (migration 067), `lib/attacker-name-resolve.js` (abstain-not-guess), extractor + promote support. Backend model; live serving deferred to publish. Sub-project A of the GRIN-narrative enrichment.
* **Addition**: [GRIN-narrative enrichment](/pipelines/grin-narrative-enrichment.md) pipeline (sub-project B) — `grin-narrative-batch.js` (subject-pinned selection), `.claude/agents/grin-extractor.md` (subject-pinned extractor), `grin-narrative-stage.js` (attacker-resolution abstain gate, `regional_context='Global'`, `ai_vouch_status='uncertain'`). Reuses the multi-critic → promote rails unchanged. Phase-1 tooling; operational run + Phase-2 traits deferred.

## 2026-06-24
* **Addition**: [entity-dedup](/pipelines/entity-dedup.md) review surface (follow-on #2) — agent-batch reviewer (`dedup-review-batch-{prepare,import}.js`, `lib/dedup-critic-prompts.js`, `entity_dedup_verdicts` migration 066) routes each pending candidate to one specialty critic for a same-taxon verdict; a 0.8 gate merges/rejects/escalates; `/api/admin/dedup/entities*` + an admin tab resolve escalations.

## 2026-06-23
* **Hardening**: Added three unreliable-epithet guards to [entity-dedup](/pipelines/entity-dedup.md) `tierOf` (hybrid-marker-as-epithet, placeholder/morphospecies code, cross-genus mismatch) after the first operational drain measured a ~15% false-positive rate in `auto_safe` from the sweep's 2nd-token distance breaking on those classes. Cut the rate to a small distance-1 real-species residual (`Rubus microphyllus`/`macrophyllus`), now an explicit known gap routed through review rather than blind-drained.
* **Update**: Recorded two [entity-dedup](/pipelines/entity-dedup.md) classifier gaps (follow-on #2) surfaced by the first production merge (`Brassica oleracea var. capitata` → the served canonical `Brassica oleracea capitata`, same `gbif_key`): `structuralNorm` doesn't strip infraspecific rank markers (`var.`/`subsp.`/`f.`) so that duplicate class slips both detectors; and `pickCanonicalForDedup` falls through to data-mass on same-`gbif_key` pairs (should prefer served+classified).
* **Addition**: New [entity-dedup](/pipelines/entity-dedup.md) pipeline concept — precision tiering (`lib/dedup-tier.js`) over `entity_dedup_candidates`, slug-collision ingest, and the reversible `auto_safe` drain (`entity_dedup_log` migration 065). Documented `entities.merged_into_entity_id`.
* **Initialization**: Created the AEDIN knowledge bundle — whole-platform scope. Established the directory structure ([architecture](/architecture/index.md), [pipelines](/pipelines/index.md), [datasets](/datasets/index.md), [services](/services/index.md), [decisions](/decisions/index.md)) and the root [index](/index.md) + [overview](/overview.md).
* **Creation**: 18 concepts capturing the backend/web architecture, the four data pipelines (incl. the [variety-intake](/pipelines/variety-intake.md) subsystem and its 2026-06-23 GRIN gate-less reconciliation), the `entities`/`claims`/`varieties` data models, the two services, and three durable decisions.
* **Adoption**: Established the bundle as the canonical *descriptive* project map in **permanent maintain mode** — mandate added to `CLAUDE.md` (first section, "Project knowledge map") + a project memory. Adopted the **durable-not-volatile** rule and stripped live magnitudes (entity/claim/variety counts) from the concepts; structure, relationships, design constants, and historical-event records stay — query the DB for current counts.
* **Update**: Documented `entities.slug` (the URL page key + its `slugify` rule + the collision-aware backfill) in [entities](/datasets/entities.md) and the slug-at-promote step in [variety-intake](/pipelines/variety-intake.md). The slug backfill made the served varieties (incl. landraces) servable and flagged duplicate-taxa slug-collisions (the `×`-hybrid pairs) `needs_dedup` as a dedup worklist.
* **Decision**: Added [knowledge-systems boundary](/decisions/knowledge-systems-boundary.md) — defines what goes where across the four coexisting stores (`CLAUDE.md` imperative rules, this `.okf/` descriptive map, `docs/journal.md` chronological log, `.claude` memory) so they cross-link instead of duplicating. Companion CLAUDE.md fixes in the same pass: corrected the migration count (`001…018` → `001…061+, ~86 files`), replaced the non-existent `migrate:planner` command, and refreshed the "Known gaps" as-of date.
